# Goal: Bootstrap CHODAE KUNG Implementation Harness

Read `CLAUDE.md`, `prd.md`, and the entire relevant `harness/` guidance.
Inspect the repository and all available Genspark outputs under `design/genspark/`.

Do not implement product features yet.

Your goal is to prepare the project for reliable design-to-code work by:

1. running the harness preflight,
2. mapping all design artifact locations into `harness/state/design-source-map.md`,
3. identifying existing tokens/components in the codebase,
4. creating/updating `harness/state/component-registry.md`,
5. identifying lint/typecheck/test/build commands,
6. recording only material setup gaps in `harness/state/implementation-log.md`.

Do not redesign anything. Do not create replacement components if Genspark or the codebase already defines them.
